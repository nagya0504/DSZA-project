DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `addAdmin`(IN `username` VARCHAR(50), IN `familyName` VARCHAR(100), IN `givenName` VARCHAR(100), IN `email` VARCHAR(50), IN `pwd` VARCHAR(50), IN `phone` VARCHAR(15), IN `town` VARCHAR(100))
INSERT INTO `user`
VALUES(NULL, username, familyName, givenName, email, SHA2(pwd, 256), phone, town, 1, NOW(), NOW(), NULL)$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `addBook`(IN `user_id` INT, IN `title` VARCHAR(100), IN `author` VARCHAR(100), IN `price` INT, IN `description` TEXT, IN `img` VARCHAR(100))
INSERT INTO `product`
VALUES(NULL, user_id, title, author, price, description, img, 1, NOW())$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `addFav`(IN `userId` INT, IN `productId` INT)
INSERT INTO `favourites`
VALUES (NULL, userId, productId)$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `addUser`(IN `username` VARCHAR(50), IN `familyName` VARCHAR(100), IN `givenName` VARCHAR(100), IN `email` VARCHAR(50), IN `pwd` VARCHAR(50), IN `phone` VARCHAR(15), IN `town` VARCHAR(100))
INSERT INTO `user`
VALUES(NULL, username, familyName, givenName, email, SHA2(pwd, 256), phone, town, 2, NOW(), NOW(), NULL)
-- regisztráció$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `deleteBook`(IN `id` INT)
DELETE FROM `product`
WHERE `product`.`id` = id$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `deleteFav`(IN `userId` INT, IN `bookId` INT)
DELETE FROM `favourites`
WHERE `favourites`.`user_id` = userId AND `favourites`.`product_id` = bookId$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `deleteUser`(IN `id` INT)
DELETE FROM `user` WHERE `user`.`id` = id$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `login`(IN `email` VARCHAR(50), IN `pwd` VARCHAR(255))
BEGIN
    DECLARE user_count INT;

    SELECT COUNT(*) INTO user_count
    FROM `user`
    WHERE `user`.`email` = email AND `user`.`pwd` = SHA2(pwd, 256);

    IF user_count = 1 THEN
        SELECT 'Login successful' AS result;
    ELSE
        SELECT 'Login failed' AS result;
    END IF;
END$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `orderBooksAZ`()
SELECT * FROM `product` ORDER BY `product`.`title` ASC$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `orderBooksAsc`()
SELECT * FROM `product` ORDER BY `product`.`price` ASC$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `orderBooksDesc`()
SELECT * FROM `product` ORDER BY `product`.`price` DESC$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `orderBooksZA`()
SELECT * FROM `product` ORDER BY `product`.`title` DESC$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `selectAllAdmin`()
SELECT * from `user` WHERE `user`.`role_id` = 1$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `selectAllBooks`()
SELECT * FROM `product`$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `selectAllUser`()
SELECT * from `user` WHERE `user`.`role_id` = 2$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `selectProduct`(IN `id` INT)
SELECT * FROM `product` WHERE `product`.`id` = id$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `updateBookAuthor`(IN `id` INT, IN `newAuthor` VARCHAR(100))
UPDATE `product`
SET `product`.`author` = newAuthor
WHERE `product`.`id` = id$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `updateBookDesc`(IN `id` INT, IN `newDesc` TEXT)
UPDATE `product`
SET `product`.`description` = newDesc
WHERE `product`.`id` = id$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `updateBookImg`(IN `id` INT, IN `newImg` VARCHAR(100))
UPDATE `product`
SET `product`.`image` = newImg
WHERE `product`.`id` = id$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `updateBookPrice`(IN `id` INT, IN `newPrice` INT)
UPDATE `product`
SET `product`.`price` = newPrice
WHERE `product`.`id` = id$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `updateBookStatus`(IN `id` INT, IN `newStatus` INT)
UPDATE `product`
SET `product`.`status_id` = newStatus
WHERE `product`.`id` = id$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `updateBookTitle`(IN `id` INT, IN `newTitle` VARCHAR(100))
UPDATE `product`
SET `product`.`title` = newTitle
WHERE `product`.`id` = id$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `updateUserPassword`(IN `id` INT, IN `newPwd` VARCHAR(50))
UPDATE `user`
SET `user`.`pwd` = newPwd
WHERE `user`.`id` = id$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `updateUserPhone`(IN `id` INT, IN `newPhone` VARCHAR(15))
UPDATE `user`
SET `user`.`phone` = newPhone
WHERE `user`.`id` = idIN$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `updateUserTown`(IN `id` INT(11), IN `newTown` VARCHAR(100))
UPDATE `user`
SET `user`.`town` = newTown
WHERE `user`.`id` = id$$
DELIMITER ;

DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `updateUserUsername`(IN `id` INT(11), IN `newUsername` VARCHAR(50))
UPDATE `user`
SET `user`.`username` = newUsername
WHERE `user`.`id` = id$$
DELIMITER ;
